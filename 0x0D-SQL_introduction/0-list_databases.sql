-- all Database in-- Create Database

create database if not exists hbtn_0c_0;-- Char-length * order
SELECT score, name FROM second_table WHERE CHAR_LENGTH(name) > 0 ORDER BY score DESC;
 a MySQL Server
SHOW DATABASES;
